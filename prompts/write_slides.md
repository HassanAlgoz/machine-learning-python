For each of the notebook, extract the key main ideas.
Then, convert to talking points.
Then, convert that into quarto qmd slides following a similar way that other `*_slide.qmd` files done.
Progressively build ideas one after the other, step by step.
For slides with images, use two columns with 4:6 ratio between content and image.
Look at: content/modules/M1/lessons for the notebook files to make corresponding slides from them.